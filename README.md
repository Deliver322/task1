# task1 Innowwise courses
Fix validator, parser should return List<int[]> for Factory
move code from "validator" to FactoryImpl to check array for null ect
in services use validation template to execute boilerplate code(во всех 
сортировках должна быть проверка на null и лучше использовать 

